{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
<br>
<br>
 <form name="frm" method="post" action="">
        <h1>Product Management</h1>
        <p>
          <img src="image/add.png" alt="Add new" width="16" height="16" border="0" /> <a href="{{path('addProduct')}}"> Add New</a>
        </p>
        <table id="tableproduct" class="table table-striped table-bordered" cellspacing="0" width="100%">
            <thead>
                <tr>
                    <th><strong>No.</strong></th>
                    <th><strong>Product Name</strong></th>
                    <th><strong>Price</strong></th>
                    <th><strong>Description</strong></th>
                    <th><strong>Quantity</strong></th>
                    <th><strong>Category</strong></th>
                    <th><strong>Image</strong></th>
                    <th><strong>Delete</strong></th>
                </tr>
            </thead>
            <tbody>
            {% for p in product %}
                 <tr>
                         <td class="text-center">{{p.id}}</td>
                         <td>{{p.Pro_Name}} </td>
                         <td>{{p.Price}}   </td>
                         <td>{{p.Pro_Desc}} </td>
                         <td>{{p.Pro_qty}}  </td>
                         <td>{{p.Cat_Name}}     </td>
                        <td align='center'>
                            <img src='{{path('get_image', { filename: p.Pro_Image }) }}' border='0' width="50" height="50" />
                        </td>
                            <td align='center'>
                                <a href="{{path('deleteProduct', {id:p.id})}}" onclick="return deleteConfirm()">
                                    <img src="image/delete.png" border='0' width="16" height="16" /></a>
                            </td>
                    </tr>
            {% endfor %}
            </tbody>
        </table>
    </form>
{% endblock %}
